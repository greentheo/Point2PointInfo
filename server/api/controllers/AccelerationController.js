/**
 * AccelerationController
 *
 * @description :: Server-side logic for managing Accelerations
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {
	// we may not actually need this, as Acceleration data is only supposed to be contained (?)
  // within a location datapoint
};

